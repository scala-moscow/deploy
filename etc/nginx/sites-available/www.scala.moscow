server {
    listen 80 default_server;
    listen [::]:80 default_server ipv6only=on;

    root /home/nikita/scala.moscow/public;  # temp
    server_name scala.moscow localhost;

    location / {
        index index.html index.htm;
        root /home/nikita/scala.moscow/public;  # temp
    }

    location ~ /\. {
        deny all;
    }
}
