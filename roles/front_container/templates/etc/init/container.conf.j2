description "Container {{ container.name }}"
author "Scala Moscow"
start on filesystem and started docker
stop on runlevel [!2345]
respawn
script
  /usr/bin/docker start -a {{ container.fqn }}
end script
